# Bootstrap TODO — Cloud Deploy

This OpenClaw instance was deployed to AWS. Most setup is already handled.
Work through any remaining items below.

## 1. Personalization
- [ ] Pick a persona at https://righthands.dev (or say "skip" for default)
- [ ] Customize `USER.md` with owner's name, location, preferences
- [ ] Review `AGENTS.md` and adjust rules as needed

## 2. Google Workspace (if configured)
- [ ] Connect Google account via GOG CLI (the agent will guide you through this)

## 3. Cron Jobs (optional)
- [ ] Set up recommended cron jobs (self-reflection, system-watchdog)
  - Ask: "Set up recommended cron jobs"

## 4. Cleanup
- [ ] Delete this bootstrap folder: `rm -rf ~/.openclaw/workspace/bootstrap`
- [ ] Remove the bootstrap reference from AGENTS.md
