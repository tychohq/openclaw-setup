# Environment files (keep examples)
.env*
!.env.example

# Secrets (filled-in config files at repo root)
openclaw-secrets.*
openclaw-auth-profiles.json

# Terraform
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
tfplan
*.tfvars
!*.tfvars.example
!*.tfvars.advanced.example

# Slack local state
.slack-app-state.json

# tldr
.tldr/
.tldrignore

# OS
.DS_Store

# Checklist local config (keep example)
shared/checklist/checklist.conf
tmp-current-config.json
tmp-default-config.json

# Discord channel mapping for stop hook notifications
.discord-channel

# Generated backend config (S3 state)
backend.tf
node_modules/
