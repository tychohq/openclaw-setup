id: browser-config
description: "Auto-detect headful/headless browser mode based on display availability"
targets: ["*"]
created: 2026-03-01T22:10:00Z

steps:
  - type: exec
    command: test "$(uname)" = Darwin -o -n "${DISPLAY:-}" && openclaw config set browser.headless false || openclaw config set browser.headless true
