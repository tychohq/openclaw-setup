id: memory-config
description: "Memory backend, vector search, session memory hooks"
targets: ["*"]
created: 2026-03-01T22:01:00Z

requires:
  - OPENAI_API_KEY

steps:
  - type: config_patch
    merge_file: configs/memory-config.json
