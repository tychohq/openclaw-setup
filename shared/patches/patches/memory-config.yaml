id: memory-config
description: "Memory backend, vector search, session memory hooks. Requires OPENAI_API_KEY."
targets: ["*"]
created: 2026-03-01T22:01:00Z

steps:
  - type: config_patch
    merge_file: configs/memory-config.json
