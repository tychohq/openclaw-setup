id: memory-config
description: "Builtin memory backend (.md indexing), session memory hooks, OpenAI embeddings"
targets: ["*"]
created: 2026-03-01T22:01:00Z

requires:
  - OPENAI_API_KEY

steps:
  - type: config_patch
    merge_file: configs/memory-config.json
